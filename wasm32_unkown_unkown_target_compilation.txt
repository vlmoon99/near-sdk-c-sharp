Prerequisites :
[
- Ubuntu x86/64 or arm (native or wsl based) 
- Dotnet 10 [https://dotnet.microsoft.com/en-us/download/dotnet/10.0]
- Wasi SDK 27 (Dowload from releases and unzip , in the future u will use clang++ from there and it also will be nesessary for dotnet to wasi compilation) [https://github.com/WebAssembly/wasi-sdk]
- wasm-tools, wat2wasm tools (Install it using via rust and cargo or find pre-built bin's)
- near cli [https://docs.near.org/tools/near-cli#installation]
]

Steps :

1. Create empty project
```bash
dotnet new console -n ContractProof
```

2.Configure csproj file

2.1 Set output type from

```xml
    <OutputType>Exe</OutputType>
```
to

```xml
    <OutputType>library</OutputType>
```

2.2 Add nesessary props for small size wasm output 

```xml
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <DebugType>none</DebugType>
    <InvariantGlobalization>true</InvariantGlobalization>
    <StackTraceSupport>false</StackTraceSupport>
    <OptimizationPreference>Size</OptimizationPreference>
    <EnableTrimAnalyzer>true</EnableTrimAnalyzer>
    <ILLinkTreatWarningsAsErrors>false</ILLinkTreatWarningsAsErrors>
    <TrimmerRemoveSymbols>true</TrimmerRemoveSymbols>
    <DebuggerSupport>false</DebuggerSupport>
    <PublishTrimmed>true</PublishTrimmed>
    <SelfContained>true</SelfContained>
    <IlcGenerateMstatFile>true</IlcGenerateMstatFile>
    <IlcGenerateDgmlFile>true</IlcGenerateDgmlFile>
```

2.3 Add Native AOT LLVM Compiler to the project

```xml
  <ItemGroup>
    <PackageReference Include="Microsoft.DotNet.ILCompiler.LLVM" Version="10.0.0-*" />
    <PackageReference Include="runtime.$(NETCoreSdkPortableRuntimeIdentifier).Microsoft.DotNet.ILCompiler.LLVM" Version="10.0.0-*" />
  </ItemGroup>
```

2.4 Add WASI stubs for our build system , because if we will not have clean env imports in our wasm file - this file cannot to be deployed on the Near Blockchain. 
Only Near Blockchain env imports can be in WASM file. U can also use this tactics in other fileds when u will need to have clean env functions.
Also here we are define our WIT(Wasi Interface Type) for strcit import and export for our clean wasm compilation.

```xml
  <ItemGroup>
    <NativeLibrary Include="stubs/stubs.o" />
    <CustomLinkerArg Include="-Wl,--component-type $(MSBuildProjectDirectory)/wasi_interfaces_type/smartcontract.wit" />
  </ItemGroup>
```

2.5 Add nuget.config with next properties

```xml
<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <packageSources>
    <clear />
    <add key="nuget" value="https://api.nuget.org/v3/index.json" />
    <add key="dotnet-experimental" value="https://pkgs.dev.azure.com/dnceng/public/_packaging/dotnet-experimental/nuget/v3/index.json" />
  </packageSources>
</configuration>
```


It was the initital setup of the project, in the future we will return in our csproj file for future optimizations of wasi build


3.Write Smart Contract which will later on we will compile to the clean wasm target. If u are new to the Blockchain technology and smart contract developemnt, I advie to read a little bit about it on the Near Blockchain docs website Basics[https://docs.near.org/protocol/basics] Smart Contract Developemnt[https://docs.near.org/smart-contracts/what-is].
But I Will try to make it simple  to write this few sentences for fully underestending what we will write the next :
3.1.Each Smart Contract represents a code which stores on the Blockhain nodes and other users can execute this code.
3.2.Each Smart Contract has some sort of native functions (aka native functions(File System, Networing, Time, Cryptography) on Linux,Windows,Mac os ) and u can use this Blockhcain native Fucntions inside smart contract
3.3.The basic things which blockhcain can do insdie smart contract functions - 
[- write/read data, 
 - call native blockhcain cryptography functiosn , 
 - call other smart contracts, 
 - get block time, 
 - get tx information (who sign tx , who will recive it , how much gas user has to spend, how much deposit user attatch to the tx, what the input of the user)
]

4.Each Tx on a chain cost money(Near Tokens) in one word it called Gas which is means how much computatonal resources blockchain node spends in order to execute your functions, each data which was stored also cost tokens which is 100kb per NEAR token .
So taking it into account we need to write our functions small, efficient, for cheapest and fastest transaction.


After a little theoretical inforamtion we are able to get into writing our first smart contract code.

```csharp
using System;
using System.Runtime.InteropServices;

namespace ContractProof;

public unsafe static class SmartContract
{
    public const string owner = "vlmoon.near";

    [UnmanagedCallersOnly(EntryPoint = "returnowner")]
    public static void ReturnOwner() => NearSmartContractBuilder.ReturnMethod(owner);

    [UnmanagedCallersOnly(EntryPoint = "returnvalue")]
    public static void ReturnValue() => NearSmartContractBuilder.ReturnMethod("hello world");

    [UnmanagedCallersOnly(EntryPoint = "helloworld")]
    public static void HelloWorld() => NearSmartContractBuilder.LogMethod("hello world");

    [UnmanagedCallersOnly(EntryPoint = "returnvalueinput")]
    public static void ReturnValueInput()
    {
        NearSmartContractBuilder.Execute(() =>
        {
            string input = NearSmartContractBuilder.GetInputString();
            NearSmartContractBuilder.ReturnValue(input);
        });
    }

    [UnmanagedCallersOnly(EntryPoint = "loginput")]
    public static void LogInput()
    {
        NearSmartContractBuilder.Execute(() =>
        {
            string input = NearSmartContractBuilder.GetInputString();
            NearSmartContractBuilder.Log($"Received input: {input}");
        });
    }

    [UnmanagedCallersOnly(EntryPoint = "greet")]
    public static void Greet()
    {
        NearSmartContractBuilder.Execute(() =>
        {
            string input = NearSmartContractBuilder.GetInputString();
            string response = $"Hello, {input}!";
            NearSmartContractBuilder.ReturnValue(response);
        });
    }

    [UnmanagedCallersOnly(EntryPoint = "store")]
    public static void Store()
    {
        NearSmartContractBuilder.Execute(() =>
        {
            string input = NearSmartContractBuilder.GetInputString();
            NearSmartContractBuilder.StorageWrite("mykey", input);
            NearSmartContractBuilder.Log($"Stored: {input}");
        });
    }

    [UnmanagedCallersOnly(EntryPoint = "retrieve")]
    public static void Retrieve()
    {
        NearSmartContractBuilder.Execute(() =>
        {
            string value = NearSmartContractBuilder.StorageRead("mykey");
            NearSmartContractBuilder.ReturnValue(value);
        });
    }
}
```

5. Smart Contract Structure :

5.1.Basic Classes on top of which all things works in smart contract building  which include 3 leveles.

```csharp
class NearSmartContractBuilder {} - helpful class for building smart contract , level 3 in this example
class NearBlockchainEnv {} - implemented enviroment methods of the Near Blockchain , based on NearSystemImports
class NearSystemImports {}  - Near Blockchain System Method mappings, get system methods of the Bear Blockhain which we can call inside our Smart Contract
```

5.2.Smart contract structure

```csharp
public unsafe static class SmartContract
{
    public const string owner = "vlmoon.near";
    ... other  smart contract structure fields ,
    each fields cost some storage to store, more u wanna store the more u need to pay for ur smart contract
}
```

5.3.Smart Contract functions

On chain we have 2 types of the functiosn - view (read any data) - it's free for every client

```csharp
public unsafe static class SmartContract
{
    public const string owner = "vlmoon.near";

    [UnmanagedCallersOnly(EntryPoint = "returnowner")]
    public static void ReturnOwner() => NearSmartContractBuilder.ReturnMethod(owner);
}
```

And we have write operation (any modifications of the Smart contract storage) whihc must to be paid some sort of gas for the transaction + for the storage which u will ocipied,
some smart contracts can pay for the user, some smart contract required to attach some mort of the deposit in order t ostore something on a chain.
In case whene u will delete something from smart contract - u will be able to widtrawl your deposit token for storage from the smart contract.

```csharp
    [UnmanagedCallersOnly(EntryPoint = "store")]
    public static void Store()
    {
        NearSmartContractBuilder.Execute(() =>
        {
            string input = NearSmartContractBuilder.GetInputString();
            NearSmartContractBuilder.StorageWrite("mykey", input);
            NearSmartContractBuilder.Log($"Stored: {input}");
        });
    }

    [UnmanagedCallersOnly(EntryPoint = "retrieve")]
    public static void Retrieve()
    {
        NearSmartContractBuilder.Execute(() =>
        {
            string value = NearSmartContractBuilder.StorageRead("mykey");
            NearSmartContractBuilder.ReturnValue(value);
        });
    }
```

6.Build Code into clean wasm-32-unkown-unkown target

6.1.Make our build script executable
```bash
chmod +x ./scripts/build_smart_contract.sh
```

6.2.Run build script
```bash
./scripts/build_smart_contract.sh
```

6.3.Inside build script 

We have a few things whic his related to our Blockchain use case , if u are trying to create clean wasm u dont need to follow this script 1 to 1.

In this script we unbundled our wasi module , take main module with code , change "-" to "_" in improts, it's the WASI limitation we can create env imports with "_",
transform it back from wat to wasm and vaulya our clean wasm file is ready. we ahve our contract.wasm file whic hsi ready to deploy on the chain

7.Deploy Smart Contract

Before we are gonna deploy our smart contract on chain we must to know a few thigns, for now Near blockhain on the mainnaet adn testnet not support bulk memmory wasm feature,
it's a reason why right after deploy this samrtcontat.wasm on chain u will have "Deserialization" error, it will be fixed in the new releasese very soon, and for now for testing 
the idea on C# on a chain we will use localnet network (aka run Near Blockchain local node).

In order to do it start wit hthe next steps :
[
7.1.Install Rust

7.2.Check your RAM avalability (if less than 64gb - create swap 64gb) , because on the last steps of bulding the NearBlockchain node u willl have an error.

7.3.Clone near core 

```bash
git clone https://github.com/near/nearcore
```

7.4.Build Debug or Release 

```bash
cargo build
```

```bash
cargo build --release
```

7.5.Run Near Node

Go to the bin folder and find neard bin
```bash
cd target/debug/
```

this will init all necessary configs in order to run node localy
```bash
./neard init
```

this cmd will run our local node , after we run our node for now we need jsut to check that all things run and we can shut down it for now , because we need to create
some configuration. 
```bash
./neard run
```

As we alredy downlaod and installed near cli in Prerequisites , so we have it instialed and we can create our configurations

We need to setup our cli in order to be able to connect to our local node:

```bash
cd ~/.config/near-cli
```

```bash
nano config.toml
```
Add this , or if there will be some config - change it to your localhost - http://127.0.0.1:3030

```config
[network_connection.localnet]
network_name = "localnet"
rpc_url = "http://127.0.0.1:3030"
wallet_url = "https://app.mynearwallet.com/"
explorer_transaction_url = "https://explorer.near.org/transactions/"
```

7.6.Create keys for smart contract manipulation (deploy , call functions , etc)

```bash
cd ~/.near/
```
in this folder we will have the file validator_key.json

We will need to copy all data in new file call test.near.json + we need to add new key "secret_key" with value of private key , 
it will be necessary for cli in order to sign tx (You can find an example in ContractProof/near_creds/test.near.json , 
but there a keys from my local node, pls take it into account), 
after we will create this file we can tranfer it to our smartcontract/test-wasm folder 
(take into account that there alredy some keys with the same names - you will need to delete it before moving your keys).

7.7.Run our local node again 
```bash
./neard run
```

7.8.Deploy our smart contract on the localchain
```bash
chmod +x ./scripts/deploy_smart_contract.sh
```

```bash
./scripts/deploy_smart_contract.sh
```
]

8.Test Smart Contract using Near CLI

Make our test executable . Pls make sure u have near cli installed  and test.near.json keys was generated properly with secretKey key where your private key will be.
```bash
chmod +x ./scripts/test_smart_contract_functions_execution.sh
```

Execute our test, insdie tests we have Read only (free) transactions, and Paybale trasactiosn for read and write operations

```bash
./scripts/test_smart_contract_functions_execution.sh
```
